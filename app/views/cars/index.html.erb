<h1>Frota</h1>

<% if @cars.any? %>
  <% if current_user.admin? %>
    <%= link_to 'Registrar novo veiculo', new_car_path %>
  <% end %>
  <br>
  <br>
  <table>
    <tr>
      <th>Modelo</th>
      <th>Cor</th>
      <th>Placa</th>
      <th>Quilometragem</th>
      <th>Filial</th>
    </tr>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.car_model.name %> <%= car.car_model.motorization %> <%= car.car_model.fuel_type %> - <%= car.car_model.year %></td>
        <td><%= car.color %></td>
        <td><%= car.license_plate %></td>
        <td><%= car.mileage %></td>
        <td> <%= link_to "#{car.subsidiary.name}", car_path(car) %></td>
    </tr>
    <% end %>
  </table>

<% else %>
  <% if current_user.admin? %>
    <h3>Não existem frotas de veículos cadastradas. <%= link_to 'Clique aqui', new_car_path %> para cadastrar um novo veículo para a frota</h3>
  <% else %>
    <h3>Não existem frotas de veículos cadastradas.</h3>
  <% end %>
<% end %>
<br>

<%= link_to 'Voltar', root_path %>